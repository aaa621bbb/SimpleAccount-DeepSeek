# v2.31.3 变更摘要

## 一、诚实边界（放宽「无法执行」禁令）
- 允许如实说明能力边界 / 无法执行；写操作仍以【账本已核验】+ rows_affected 为唯一成凭，无据不得报成。

## 二、端侧与云端统一协议
- `OnDeviceModelBackend.compactPrompt = false`，走完整 system + AgentLoop。
- 移除劣质「本地精简规划器」复读；改为 **协议桥**（消费完整 messages，产出 tool_calls）。

## 三、端侧选型与下载
- 按档 ≥10 款候选，名称含量化（如 Qwen3 4B Q8_0）；入门/标准/进阶/旗舰。
- 多镜像下载；AI 设置与设置页 **显性「管理端侧模型」** 入口。

## 四、图标与分类创建
- 二级图标语义纠正（水果→nutrition）；图标库 label；`nextFreeIcon` 未占用分配。
- `create_category` / `create_sub_category` 支持 icon_name；记一笔新增分类可选图标。

## 五、筛选弹层
- 分类筛选改为全量列表 + 二级展开；全站（除统计）BottomSheet `skipPartiallyExpanded`。

## 六、统计二级
- 开关：二级全拆 / 合并一级；合并时仍可展开二级；账单列表带分类图标。

## 七、月底约
- 测算详情防御加载 + 安全返回导航。

## 版本
- versionName 2.31.3 / versionCode 51
