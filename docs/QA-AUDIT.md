# 2.27 全量端到端审查

审查范围：叠在主干 2.26.0 上。①管家虚假完成 / 无据断言；②识图记账全链路空结果；③思考链默认折叠与视口锚定。已知问题闭环后发 **2.27.0（versionCode 44）**。

本环境 `ANDROID_HOME` 为空，不能编 APK / 跑真机。结论来自静态审查。回滚点：本分支父提交。本会话只推 `arena/01a06d4a-simpleaccount-deepseek`，不直推主干、不 force-push。

## 1. 核心链路

| 链路 | 结果 | 说明 |
| --- | --- | --- |
| 对象级改分类 | 通过 | 本地认「商家+五十元→居住，其余→餐饮」；写完回读核验 |
| 管家禁止假完成 | 通过 | 写意图未调写工具则拦截「已完成/已改好」；工具结果带【账本已核验】 |
| reclassify 立刻写库 | 通过 | 对象级不再走 NEED_CONFIRM；整商户 classify_merchants 仍需确认 |
| 识图 JSON | 通过 | 数组/单对象/中文键/金额字符串；空结果不再说「不够清晰」 |
| 识图画质 | 通过 | JPEG 90、宽度采样门槛 1440；prompt 禁止无金额就输出 [] |
| 思考链折叠 | 通过 | 默认收起；点击只展开该回合；发图会清掉上一回合 reasoning |
| 2.26 既有项 | 通过 | 差缺条、搜索 caret、识图 max_tokens≤1024、写路径关思考不改用户档 |

## 2. 异常路径

| 场景 | 处理 |
| --- | --- |
| 模型口头「已完成」未调工具 | 回复改为「账本没有改动」并给出可执行说法 |
| 写库后分类对不上 | 工具返回核验失败，不报成功 |
| 商家>8 笔且无金额/流水号 | 本地拒绝并说明，不整商户一刀切 |
| 识图 HTTP/超时 | 展示真实错误，不当成「图里没有账单」 |
| 模型返回散文 | 宽松抽金额；仍空则贴原文片段 |
| 新消息/发图 | `reasoning=null`，历史思考链保持各自折叠状态 |

## 3. 否决项核对

- 未用换配色充当重构。
- 未把日历栅格作默认。
- 「固定」仍下架。
- 写账必须落库或明示无法执行，禁止假完成。
- 思考档用户仍可自选；仅写回合关思考。
- 思考链不默认撑开视口。

## 结论

静态审查通过。版本 **2.27.0 / 44**。真机请核：天津职业技术师范大学 50 元改居住其余改餐饮是否落库；清晰截图不再空识别；点开思考只动一条气泡。
