# SimpleAccount-DeepSeek 项目交接文档

> 交接日期：2026-08-17
> 交接对象：接手本项目的 AI 开发者（后续简称"接任者"）
> 项目仓库：`https://github.com/aaa621bbb/SimpleAccount-DeepSeek.git`（**private**）
> 当前 HEAD：`40971d2`（v2.2.0 版本号未升，实际功能已到 v2.5）

---

## 0. 一句话概况

这是一个 **Android 记账 App**（Kotlin + Jetpack Compose + Room + Hilt），核心特色是：
**微信/支付宝账单导入** + **AI 智能会计 Agent（内嵌 function calling）** + **商家归类映射表** + **无需 USB 的日志抓取**。

最近一轮（v2.3→v2.5）把 AI 从"被动问答"升级成了"**会自己查库、算账、归类的 Agent**"，并重做了 AI 服务商设置界面。**Agent 代码已编译进 APK 但尚未在真机验证 V4 Flash 的 function calling 是否稳定**——这是接任者第一优先要做的验证。

---

## 1. 技术栈

| 项 | 值 |
|---|---|
| 语言 | Kotlin 1.9.22 |
| UI | Jetpack Compose (BOM 2024.02.00, KotlinCompiler 1.5.10) |
| 架构 | MVVM + Hilt 2.50（DI）+ Room 2.6.1（数据库）|
| 导航 | androidx.navigation:navigation-compose 2.7.7 |
| 网络 | OkHttp 4.12.0（仅用于 AI API） |
| Excel 解析 | Apache POI 5.2.5 + poi-ooxml（微信/支付宝 xlsx 导入）|
| 加密 | androidx.security:security-crypto（AI Key 存储）|
| JSON | org.json（Android 内置）|
| minSdk / targetSdk | 26 / 34 |
| JDK | 17 |
| Gradle | 8.4（wrapper）|
| 构建 | GitHub Actions（`assembleDebug`，产物 artifact）|

**注意：项目不用协程 Flow 做全部状态管理（部分用），主要用 StateFlow + MutableStateFlow。**

---

## 2. 工程结构（源码）

```
app/src/main/java/com/simpleaccount/app/
├── SimpleAccountApp.kt          # Application：AppLog 初始化 + 崩溃捕获 + 预置分类灌入
├── MainActivity.kt
├── data/
│   ├── agent/                   # ★ 智能会计 Agent（v2.4 新增，核心！）
│   │   ├── AgentModels.kt       #   AgentToolSpec/AgentToolCall/AgentToolResult + tools→JSON
│   │   ├── AgentTools.kt        #   5 个记账工具的实现（查明细/汇总/分类统计/商家列表/批量归类）
│   │   └── AgentLoop.kt         #   ReAct 循环（≤8轮）：发消息→执行工具→回填→直到回答
│   ├── dao/                     # Room DAO（Transaction/Category/Merchant/ImportLog/ImportFailure/AiMessage/Setting）
│   ├── db/AppDatabase.kt        # DB version=3，MIGRATION_1_2 / MIGRATION_2_3
│   ├── entity/                  # 实体（Transaction 含 paymentMethod/tradeOrderNo/merchantOrderNo）
│   ├── importdata/              # ★ 账单导入（微信/支付宝）
│   │   ├── BillParser.kt        #   表头关键词识别列，xlsx(POI)/csv(GBK/UTF8) 解析
│   │   ├── CsvParser.kt
│   │   ├── ImportProcessor.kt   #   去重→分类→写库→退款抵消→ImportLog/ImportFailure
│   │   └── ParsedRow.kt         #   解析行（含支付方式/单号/来源分类）
│   ├── repository/              # Account/Merchant/Category/Settings 仓库（@Singleton）
│   ├── service/
│   │   ├── AiService.kt         # ★ OpenAI 兼容调用：chat() 纯文本 + chatWithTools() 带工具
│   │   └── ClassificationService.kt  # 分类：映射表→文件分类列→关键词→其它
│   └── di/AppModule.kt          # Hilt：DB、DAO、默认分类、IO Dispatcher
├── ui/
│   ├── addtransaction/          # 记一笔（手动）
│   ├── ai/                      # ★ AI 助手页 + AiViewModel（sendMessage 走 AgentLoop）
│   ├── home/                    # 首页（本月汇总/最近流水）
│   ├── importbill/              # 导入页 + ImportViewModel
│   ├── ledger/                  # 账本列表
│   ├── logs/                    # ★ 日志与排障（v2.3c 新增）：查看/分享日志
│   ├── merchant/                # 商家归类管理（= "映射表"，用户手动设商家→分类）
│   ├── navigation/              # Routes + NavHost
│   ├── settings/                # 设置 + AI 服务商设置（v2.5 重做，OpenMinis 风格）+ 分类/数据管理
│   ├── stats/                   # 统计（折线图）
│   └── theme/ components/       # 主题 / 共用组件
└── util/
    ├── AppLog.kt                # ★ 日志工具：Logcat + 私有目录文件 + 崩溃捕获 + 环形缓冲
    ├── CategoryPresets.kt       # 预置分类（支出10类/收入6类）
    ├── KeywordRules.kt          # 关键词分类规则 + mapSourceCategory（品类别名归一化）
    ├── DateUtil.kt / MoneyUtil.kt / IconMapper.kt
```

---

## 3. 核心功能与实现要点

### 3.1 账单导入（微信/支付宝）
- **入口**：底部导航"导入" → 选文件（xlsx/csv/txt，支持 zip 内含账单）。
- **解析**：`BillParser.parseMatrix` 用**表头关键词**识别列（不硬编码列号）：
  - 日期/收之/金额/交易对方/商品/交易分类/支付方式/交易单号/商户单号
  - 微信列：交易时间、交易类型、交易对方、商品、收/支、金额(元)、支付方式、当前状态、交易单号、商户单号、备注
  - 支付宝列：交易时间、交易分类、交易对方、商品说明、收/支、金额、支付方式、当前状态、交易订单号、商家订单号、备注
- **入库**：`ImportProcessor` 去重（date|amount|merchant|product）→ 自动分类 → 写库 → 退款抵消（同商家付款≥退款时扣减付款，找不到则记收入"退款"）→ 转账独立"转账"分类 → 失败行写 `import_failures`（用户可查失败明细）。
- **新增字段**（v2.3）：`paymentMethod`（支付方式）、`tradeOrderNo`（交易单号）、`merchantOrderNo`（商户单号）——**只存不显示**。

### 3.2 分类体系（优先级）
`ClassificationService.classifyForImport(merchant, product, sourceCategory, validCategoryNames)`：
1. **商家映射表**（Merchant 表：`user_set` 用户手动最高，`classified` 次之）
2. **文件自带"交易分类/交易类型"列**：`KeywordRules.mapSourceCategory()` 归一化后**必须在现有分类名里**才采用（防微信"商户消费"等被当分类）
3. 内置关键词规则（`KeywordRules.RULES`：餐饮→交通→购物→…）
4. 兜底"其它" + 商家置 pending

映射表 UI = 设置 → 商家归类管理（`MerchantManageScreen`），可筛选/搜索/改分类。

### 3.3 AI 智能会计 Agent（v2.4，★ 最新核心）
**架构**：`AiViewModel.sendMessage` → `AgentLoop.run(userMessage, history)` → `AiService.chatWithTools`（带 tools）→ 模型返回 `tool_calls` → `AgentTools.execute` 执行记账工具 → 结果以 `tool` 消息回填 → 循环直到模型输出最终文本。

**5 个工具**（`AgentTools.specs`）：
| 工具 | 作用 | 关键参数 |
|---|---|---|
| `query_transactions` | 查明细 | month(yyyy-MM)/category/keyword/limit(默认50) |
| `get_summary` | 收支汇总 | start/end(yyyy-MM) |
| `get_category_totals` | 按分类统计 | type(expense/income) |
| `list_merchants` | 商家列表 | pending_only(boolean) |
| `classify_merchants` | 批量归类 | mappings(JSON字符串 {"商家":"分类"}) |

**system prompt** 在 `AgentLoop.systemPrompt`，明确要求"先调工具拿真实数据，不编金额"。

**模型**：用户自己在设置里配（默认 DeepSeek，用户实际用 **DeepSeek V4 Flash**）。

### 3.4 AI 服务商设置（v2.5，OpenMinis 风格）
`AiSettingsScreen` 重做：
- 2 列服务商卡片网格（品牌色圆形图标 + 选中高亮描边）
- 区块划分：启用开关 / 选择服务商 / 接口与密钥 / 选择模型
- 5 家预设：DeepSeek(蓝)/OpenAI(绿)/通义千问(橙)/智谱GLM(紫)/Kimi(青)，图标+主色在 `AiSettingsViewModel.AI_PROVIDERS`
- 选厂商自动填 baseUrl + 首个模型；模型可用 chips 点选或自定义输入

### 3.5 日志与排障（无需 USB）
- `AppLog`：所有 `AppLog.d/i/w/e` 同时写 Logcat + `filesDir/logs/app-yyyy-MM-dd.log`（按天分文件，保留5天，单文件512KB轮转）+ 内存环形缓冲(500行)
- 全局崩溃捕获（`installCrashHandler`）
- 设置 → 日志与排障：看最近日志/历史文件/一键分享（FileProvider）
- 关键业务已打点：导入解析/入库、AI 请求/响应/归类

### 3.6 其他
- 记一笔、账本列表、统计折线图、分类管理、数据导出/清空（DataManage）
- AI 对话历史存 `ai_messages` 表，保留200条

---

## 4. 最近 6 个提交（v2.3 → v2.5，均已 push）

| commit | 内容 |
|---|---|
| `cd46f6c` v2.3 | 导入增强：支付方式/交易单号/商户单号存储；分类优先级=映射表→文件分类列→关键词；DB v2→v3 |
| `e8212b9` v2.3b | AI 上下文扩展：各月收支汇总+全部支出分类+最多300条明细（修"AI只读近两月"）|
| `6c30df2` v2.3c | 日志抓取(无需USB)+AI归类可靠性修复（实际成功数统计+商家名规范化匹配）|
| `872d838` v2.3d | 审核修正（AiViewModel 拼接写法、中文引号清理、未用 import）|
| `8aaa261` v2.4 | 内嵌智能会计 Agent（AgentLoop/AgentTools/AiService.chatWithTools/AgentModels）|
| `9fcb416` v2.5 | AI 服务商设置 OpenMinis 风格 |
| `40971d2` fix | 编译修复（AiService.execute 去 suspend；AppLog import android.os.Process）|

---

## 5. ⚠️ 已知问题 / 待办（接任者重点）

### P0：真机验证 Agent（必须做）
- Agent 已编译进 APK（`SimpleAccount-v2.5-agent.apk`，CI run 31629303463 success），**但没真机测过**。
- **最大不确定点：DeepSeek V4 Flash 的 function calling 是否稳定返回 tool_calls、参数 JSON 是否规范**。
- 验证方法：装 APK → AI 设置确认 DeepSeek+V4 Flash+Key → AI 助手问"我上个月花了多少" → 看是否真查库给数字。
- 若不稳定：可能需要给工具加 `strict` 提示、或换 `deepseek-reasoner` 测试、或修 `AiService.chatWithTools` 的消息序列。

### P1：版本号未升
- `app/build.gradle` 仍是 `versionCode 6 / versionName "2.2.0"`，功能已到 v2.5，**应升级**（如 versionCode 7 / versionName "2.5.0"）。

### P2：已知可接受的隐患（不阻断，但接任者应知道）
1. `AgentTools.getSummary`：`end` 传完整日期（yyyy-MM-dd）会拼出 `"2026-03-15-31"` 非法串 → 工具描述已约束传 yyyy-MM，正常可用。
2. `get_category_totals` 无月份参数，只能答"全部历史分类合计" → 可组合 query_transactions 按月查；后续可加 start/end 参数。
3. `AiService` 里 assistant 带 tool_calls 时 content 用 `""` 而非 null → DeepSeek 宽容接受。
4. `AiViewModel.buildSystemContext` 现在是**死代码**（sendMessage 已走 Agent），可删可留。
5. `ClassificationService.classify()`（旧方法）已无人调用，保留作兼容，可清理。

### P3：用户提过的需求方向（未做）
- **无感记账**（读微信/支付宝通知+银行短信自动记账）——用户问过 GitHub 开源项目 `DykiSensei/seamless-bookkeeping`，可作为功能方向，但需敏感权限（通知使用权/短信），且 Play 审查严。
- AI 助手界面没有"Agent 正在查账"的过程反馈（现在只有 typing 转圈），可加 tool round 提示。
- 用户要"像 Minis 那么智能"——已按用户决定用 V4 Flash；若不够可换 reasoner 级模型（预设里已有 deepseek-reasoner）。

---

## 6. 构建 / 测试

### 本地
```bash
# 需要 JDK 17 + Android SDK（compileSdk 34）
export JAVA_HOME=<jdk17路径>
./gradlew assembleDebug
# 产物：app/build/outputs/apk/debug/app-debug.apk
```

### CI
- `.github/workflows/build.yml`：push 到 main 自动构建，artifact 名 `SimpleAccount-debug`。
- 产物下载：GitHub Actions → run → Artifacts。
- API 下载 artifact：
  ```
  curl -L -H "Authorization: Bearer $GITHUB_TOKEN" -H "Accept: application/vnd.github+json" \
    "https://api.github.com/repos/aaa621bbb/SimpleAccount-DeepSeek/actions/artifacts/<id>/zip"
  ```

### 数据库
- version=3。v1→v2（import_failures 表）、v2→v3（transactions 加3列 text 默认''）。
- **改实体必须加 migration**，否则 fallbackToDestructiveMigration 会清数据（已配，但别依赖它）。

---

## 7. 关键坑（历史踩过）

1. **沙箱 JVM 起不来**（PaX/grsecurity）——本仓库在受限 Linux 沙箱里无法跑 gradle/kotlinc，只能靠 CI 编译 + 人工审查。接任者如有正常环境可本地编译。
2. **POI 依赖**：xlsx 解析需要 poi 5.2.5 + poi-ooxml + xmlbeans + commons-io/collections4/math3/compress + curvesapi + SparseBitSet + **log4j-api**（IOUtils 静态初始化需要，排除会 NoClassDefFoundError）。千万别精简掉 log4j-api。
3. **GitHub Actions 环境**：仓库 token 需 `repo` + `workflow` 权限；Actions artifact 有配额。
4. **BusyBox ash**（交接环境 shell）：不支持 `**` 递归 glob、bash 数组、`${PIPESTATUS}`；`grep --include` 不支持。
5. **明文 token**：远程 URL 曾内嵌 token（GitHub 已打码），**交接后建议轮换**；优先用环境变量 `$GITHUB_TOKEN`。
6. **用户纪律**：**改完必须审核才能 push；出 APK 前要验证**——用户多次强调，接任者务必遵守（先本地/CI 验证，给审核证据，再 push）。

---

## 8. 交接物清单

| 物 | 位置 |
|---|---|
| 完整源码工程 | `/var/minis/workspace/SA_main/`（git clone，含 .git 历史，HEAD=40971d2）|
| 打好的 APK | `SimpleAccount-v2.5-agent.apk`（28MB，sha256 c04c5e45...）|
| 仓库 | `github.com/aaa621bbb/SimpleAccount-DeepSeek`（private，main 分支）|

---

## 9. 给接任者的第一条建议

**先别写新功能。** 按 P0 真机验证 Agent：
1. 装 APK，配好 DeepSeek V4 Flash + Key；
2. 问"我上个月花了多少"看是否真查库；
3. 若 function calling 不稳，优先排查 `AiService.chatWithTools` 的请求/响应结构（可用 curl 直接打 DeepSeek API 模拟验证 tools 协议）；
4. 验证通过后再升版本号、做 P2 优化、考虑 P3 方向。
